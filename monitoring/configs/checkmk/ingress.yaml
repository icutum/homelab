---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: checkmk-ingress
  namespace: checkmk
  annotations:
    external-dns.alpha.kubernetes.io/hostname: checkmk.local.icutum.es
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: cert-manager-cluster-issuer
spec:
  ingressClassName: traefik
  rules:
    - host: checkmk.local.icutum.es
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: checkmk-helmrelease
                port:
                  number: 5000
  tls:
    - hosts:
        - checkmk.local.icutum.es
      secretName: checkmk-local-icutum-es-tls
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: checkmk-agent-ingress
  namespace: checkmk
  annotations:
    external-dns.alpha.kubernetes.io/hostname: checkmk-agent.local.icutum.es
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: cert-manager-cluster-issuer
spec:
  ingressClassName: traefik
  rules:
    - host: checkmk-agent.local.icutum.es
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: checkmk-helmrelease
                port:
                  number: 8000
  tls:
    - hosts:
        - checkmk-agent.local.icutum.es
      secretName: checkmk-agent-local-icutum-es-tls
