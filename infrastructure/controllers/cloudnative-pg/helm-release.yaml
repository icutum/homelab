---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cloudnative-pg-helmrelease
  namespace: cloudnative-pg
spec:
  interval: 10m0s
  chart:
    spec:
      chart: cloudnative-pg
      version: 0.28.1
      sourceRef:
        kind: HelmRepository
        name: cloudnative-pg-helmrepository
        namespace: flux-system
  values:
    crds:
      create: true
